{
  "id": "PROJECT_PLACEHOLDER(product-benchmark-id)",
  "suite": "product-smoke",
  "mode": "preview",
  "description": "PROJECT_PLACEHOLDER(product-benchmark-description): describe the product behavior or workflow this benchmark protects.",
  "task_file": "task.json",
  "harness_command": "{python} tools/harness_loop.py --once",
  "commands": [
    "PROJECT_PLACEHOLDER(product-validation-command): replace with a safe project validation command."
  ],
  "required_artifacts": [
    "summary.json"
  ],
  "max_latency_seconds": 300,
  "regression_policy": "fail_on_status_regression",
  "model_config": {
    "mode": "PROJECT_PLACEHOLDER(product-benchmark-mode): preview or execute.",
    "notes": "PROJECT_PLACEHOLDER(product-model-config): name model, approval, sandbox, and cost assumptions before execute-mode use."
  }
}
