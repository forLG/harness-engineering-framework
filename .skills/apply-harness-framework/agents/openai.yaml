interface:
  display_name: "Apply Harness Framework"
  short_description: "Apply this lightweight human-in-the-loop harness to a real repository."
  default_prompt: "Apply the lightweight harness framework to this repository using evidence-backed facts, explicit project placeholders for unknowns, and the structural validator."
